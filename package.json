{
  "name": "packages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "pnpm lerna publish --no-private"
  },
  "devDependencies": {
    "float-bubble": "workspace:^",
    "lerna": "^7.1.3"
  }
}