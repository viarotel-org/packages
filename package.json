{
  "name": "packages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "pnpm lerna clean",
    "release": "pnpm lerna publish --no-private"
  },
  "devDependencies": {
    "@viarotel-org/float-bubble": "workspace:^",
    "lerna": "^7.1.3"
  }
}