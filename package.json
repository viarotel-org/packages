{
  "name": "packages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm lerna run build",
    "clean": "pnpm lerna clean",
    "release": "lerna publish --no-private --loglevel verbose"
  },
  "devDependencies": {
    "@viarotel-org/float-bubble": "workspace:^",
    "lerna": "^7.1.3"
  },
  "pnpm": {
    "patchedDependencies": {
      "vue-template-compiler@2.7.14": "patches/vue-template-compiler@2.7.14.patch",
      "@vitejs/plugin-vue2@2.2.0": "patches/@vitejs__plugin-vue2@2.2.0.patch"
    }
  }
}